import { Meta, Story } from "@storybook/addon-docs/blocks";
import IconInput from "./IconInput";

<Meta
  title="IconInput"
  component={IconInput}
  argTypes={{
    placeholder: {
      control: {
        type: "text",
      },
    },
    icon: {
      control: {
        type: "select",
        options: ["search", "at-sign"],
      },
    },
    size: {
      control: {
        type: "select",
        options: ["small", "large"],
      },
    },
    label: {
      control: {
        type: "text",
      },
    },
  }}
/>

export const Template = (args) => (
  <div style={{ display: "grid", gap: "2rem" }}>
    {args.variants.map((variant) => (
      <div>
        <p style={{ marginBottom: "1rem" }}>{variant.name}</p>
        <IconInput {...variant} />
      </div>
    ))}
  </div>
);

# IconInput

<Story
  name="Default"
  args={{
    variants: [
      {
        name: "small-search",
        placeholder: "Search…",
        label: "Search",
        icon: "search",
        size: "small",
      },
      {
        name: "large-search",
        placeholder: "Search…",
        label: "Search",
        icon: "search",
        size: "large",
      },
      {
        name: "small-email",
        placeholder: "Enter an email",
        label: "Enter an email",
        icon: "at-sign",
        size: "small",
      },
      {
        name: "large-email",
        placeholder: "Enter an email",
        label: "Enter an email",
        icon: "at-sign",
        size: "large",
      },
      {
        name: "smaller-width",
        placeholder: "Enter an email",
        label: "Enter an email",
        icon: "at-sign",
        size: "large",
        width: 150,
      },
    ],
  }}
>
  {Template.bind({})}
</Story>
